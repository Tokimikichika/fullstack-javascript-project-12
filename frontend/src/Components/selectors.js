export const getCurrentChannelId = (state) => state.ui.currentChannelId;
export const selectModalIsOpened = (state) => state.ui.modal.isOpened;
export const selectModalType = (state) => state.ui.modal.type;
export const selectChannelId = (state) => state.ui.modal.extra?.channelId;
